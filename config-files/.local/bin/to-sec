#!/bin/bash
echo "$@" | 
awk -F" |:" 'BEGIN{seconds=0; } \
	{ if(NF >= 1){ seconds += $1 * 3600; } \
       	  if(NF >= 2){ seconds += $2 * 60; } \
  	  if(NF == 3){ seconds += $3; } \
	} \
	END{ print seconds; }'
